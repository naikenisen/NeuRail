#!/usr/bin/env node

// Native desktop drag helper is now handled directly in Electron main process.
// This script remains as a compatibility placeholder for external tooling hooks.

process.stdout.write('drag-helper: handled in main process\n');
